const React = require('react');

const Countdown = React.createClass({

    render: ( props ) => {
        return (
          <div>
              <h3>Countdown this alive!</h3>
          </div>
        )
    }
});


module.exports = Countdown;